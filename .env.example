# WarcraftLogs API credentials
CLIENT_ID=your_warcraftlogs_client_id
CLIENT_SECRET=your_warcraftlogs_client_secret

# Blizzard Battle.net API credentials (для получения иконок)
BLIZZARD_CLIENT_ID=your_blizzard_client_id
BLIZZARD_CLIENT_SECRET=your_blizzard_client_secret

# Database URL (Railway автоматически устанавливает эту переменную)
# Локально используется значение по умолчанию из db.py
# DATABASE_URL=postgresql://wow_user:wow_password@localhost:5432/wow_db

# SQL Debug (опционально)
SQL_DEBUG=false
